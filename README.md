
# Human-Centered Sec
Mockup authentication designs for CMP410

## Prerequisites
- Node.js

## Getting started
1. Install dependencies:
	```
    npm i
    ```

2. Start the dev server:
	```
    npm run dev
    ```

3. Open the URL shown in the terminal (usually `http://localhost:5173`).

